pipeline {
    
    agent any

    stages {

        stage("build") {
            steps {
                echo "building..."
            }
        }

        stage("push") {
            steps {
                echo "pushing..."
            }
        }

        stage("deploy") {
            steps {
                echo "deploying..."
            }
        }
    }
}